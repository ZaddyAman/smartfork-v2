# Ralph Progress Log — SmartFork v2
Started: $(date)
---

## Ralph Loop System — OpenCode-Native (Full Huntley Subagent Architecture)

### Primary Path: OpenCode TUI
- Start OpenCode in project root, switch to "ralph" agent, say "Start the Ralph loop"
- Or: `opencode --agent ralph "Start the Ralph loop"`
- Design doc: `docs/opencode-native-ralph.md`
- Loop logic lives IN the agent's system prompt (no shell script needed)

### Agent Architecture

| Agent | File | Mode | Model | Permissions |
|-------|------|------|-------|-------------|
| **ralph** | `.opencode/agents/ralph.md` | primary | Strong (deepseek) | read/glob/grep/bash/task/edit |
| **ralph-plan** | `.opencode/agents/ralph-plan.md` | subagent | opencode-go/kimi-k2.6 | read/glob/grep only |
| **ralph-worker** | `.opencode/agents/ralph-worker.md` | subagent | opencode-go/kimi-k2.6 | Full access, parallel up to 10 |
| **ralph-test** | `.opencode/agents/ralph-test.md` | subagent | opencode-go/kimi-k2.6 | bash/read only, STRICTLY 1 |

### Per-Story Flow
1. Orchestrator reads prd.json → picks next story
2. ralph-plan studies spec + codebase (gaps check)
3. ralph-worker (parallel) implements the story
4. ralph-test (exactly 1) runs ruff → mypy → pytest
5. If pass → commit, update PRD. If fail → fix → retest
6. Loop to next story

### Context Guarantee
- Each subagent spawn = fresh 170k token window
- Orchestrator's context: ~3k prompt + ~1k per story summary
- After 27 stories: ~30k tokens (17% of window)
- Orchestrator NEVER reads/writes code files — only PRD + summaries

### Fallback: Shell Scripts
- `ralph/ralph.sh` (bash/WSL/Git Bash) — `./ralph.sh --tool opencode 50`
- `ralph/ralph.ps1` (PowerShell) — `.\ralph.ps1`
- Use when headless/CI or if TUI agent unavailable

### Core Huntley Principles Applied
1. Primary agent = scheduler (minimal context usage)
2. Expensive allocation work → subagents with fresh context
3. Parallel subagents for search/write, STRICTLY 1 for tests
4. Search before implementing (don't assume not implemented)
5. Full implementations only (no placeholders)
6. Fix unrelated test failures as part of increment
7. Self-improvement: update AGENTS.md with learnings

## Codebase Patterns
- Python 3.11+, Pydantic v2, Textual TUI
- Protocols for interfaces (not ABCs)
- All models in src/smartfork/models/
- TOML config at ~/.smartfork/config.toml
- Secrets at ~/.smartfork/secrets.env
- ChromaDB embedded (PersistentClient, cosine distance)
- SQLite with WAL mode
- Loguru for all logging
- Type hints everywhere
- ruff + mypy + pytest for quality

## 2026-05-09 - US-001
- Created full directory scaffolding: all 12 sub-packages under src/smartfork/ with __init__.py
- Created tests/ directory with conftest.py
- Added LICENSE (AGPL-3.0)
- Created placeholder CLI entry point (src/smartfork/cli/commands.py) with mypy-compliant type annotations
- pyproject.toml, ruff.toml, mypy.ini, pytest.ini, .gitignore already existed and are correct per spec
- **Learnings for future iterations:**
  - mypy strict mode requires return type annotations on every function (including callbacks like typer callbacks)
  - The themes/ directory doesn't need __init__.py — a .gitkeep suffices since it holds .tcss files
  - pip install -e . can hang on Windows when resolving deps; package import test is a faster alternative
---

## 2026-05-09 - US-002
- Created 5 model files: session.py, search.py, fork.py, config.py, supersession.py
- Created protocols.py with LLMProvider, EmbeddingProvider, SessionAdapter Protocols
- Updated models/__init__.py with full re-exports and __all__
- Created 6 test files in tests/models/ covering all models and protocols (59 tests)
- Fixed pydantic version incompatibility (upgraded pydantic 2.10.4 → 2.13.4)
- **Learnings for future iterations:**
  - SessionDocument MUST be decorated with @dataclass (spec snippet omitted it but notes say use @dataclass)
  - Python Protocol class-level annotations like `agent_id: str` are NOT real attributes — use `__annotations__` to test them
  - The duration_minutes guard `session_end > session_start > 0` rejects session_start=0 — tests must use valid timestamps
  - ruff requires imports within parenthesized blocks to be alphabetically sorted
  - Use modern Python 3.11+ type hints (list[...], X | None, StrEnum) — ruff enforces these
  - pydantic version must be compatible with pydantic-core; pip install --upgrade pydantic fixes ImportError
---

## 2026-05-09 - US-003
- Created providers/llm.py: OllamaLLM, AnthropicLLM, OpenAILLM with complete() and complete_structured()
- Created providers/helpers.py: check_ollama_available(), ensure_ollama_model(), list_available_ollama_models(), recommend_model()
- Updated providers/__init__.py with full public exports
- Added openai>=1.0 and anthropic>=0.30 to pyproject.toml dependencies
- Installed openai, anthropic, instructor packages
- Created tests/providers/ with 38 tests for helpers + LLM providers
- **Learnings for future iterations:**
  - AnthropicLLM and OpenAILLM need complete_structured() to satisfy LLMProvider protocol (even if cloud providers don't need it for US-003 scope)
  - API key validation: `api_key=None` should skip init-time validation (factory sets None for cloud providers, allows lazy lookup); `api_key=""` triggers env lookup and raises if missing
  - instructor.from_openai() works with Ollama's OpenAI-compatible endpoint at localhost:11434/v1
  - When mocking OllamaLLM tests, the OLLAMA_AVAILABLE guard in __init__ requires careful patching of module-level imports
  - B904 ruff rule: ImportError must be re-raised with `raise X from err` not bare `raise`
---

## 2026-05-09 - US-004
- Created providers/embedding.py: OllamaEmbedder (instruction-aware), SentenceTransformerEmbedder, OpenAIEmbedder + get_embedder() + check_embedding_model_available()
- Created providers/guard.py: EmbeddingModelGuard with dimension compatibility check and warning messages
- Updated providers/__init__.py with all embedding exports (embedders, instructions, guard)
- Created tests/providers/test_embedding.py (41 tests) + test_guard.py (8 tests)
- 138 total tests passing
- **Learnings for future iterations:**
  - OllamaEmbedder uses instruction prefixes per doc_type (Anthropic Contextual Retrieval technique); ST and OpenAI ignore doc_type
  - get_embedder("openai") needs API key at init time; tests must patch _load_api_key to avoid CI failures
  - EmbeddingModelGuard.check_compatibility: same model name trumps dimension mismatch (returns True)
  - SentenceTransformer first-use downloads the model — tests must mock SentenceTransformer entirely
  - ollama.embed() returns {"embeddings": [[...]]} with nested list; access response["embeddings"][0]
---

## 2026-05-09 - US-005
- Implemented SmartForkConfig(BaseSettings) with TOML save/load, env var override (SMARTFORK_ prefix), secrets management
- Theme validation: 6 allowed themes (phosphor, obsidian, ember, arctic, iron, tungsten), case-insensitive
- Legacy config.json → config.toml migration with backup
- get_secret()/set_secret() with env var fallback and Unix 0o600 permissions
- Singleton get_config()/reload_config() with module-level cache
- Legacy kilo_code_tasks_path property
- Created tests/test_config.py (161 total tests passing)
- **Learnings for future iterations:**
  - pydantic v2 uses @field_validator not @validator (v1 API deprecated)
  - TOML save should use nested sections ([core], [agents], [paths], [models], [indexing], [performance]) for readability
  - When patching Path.home() in tests, must set return_value BEFORE config module is imported, else MagicMock leaks into CONFIG_DIR defaults
  - Path fields benefit from @field_validator with expanduser() to handle ~ in paths
  - agents dict uses string keys mapping to AgentConfig; TOML serialization needs special handling for nested dict of models
---
