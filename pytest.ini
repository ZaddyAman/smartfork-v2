[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = src
addopts = -v --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
